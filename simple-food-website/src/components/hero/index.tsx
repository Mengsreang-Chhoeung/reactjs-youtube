import React from "react";
import "./index.css";

interface HeroProps {}

const Hero: React.FC<HeroProps> = (props) => {
    const {} = props;

    return (
        <div className="hero"></div>
    );
}

export default Hero;
